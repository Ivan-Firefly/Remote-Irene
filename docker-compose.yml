version: '3'
services:
  irene-mic-client:
    container_name: irene-mic-client
    image: firefly27/irene-mic-client
    devices:
      - /dev/snd:/dev/snd
    volumes:
      - ./options.json:/options.json
      - ./options_telegrambot.json:/options_telegrambot.json
    restart: 'unless-stopped'
    # command: python3 run_remoteva_voskrem.py & python3 run_remote_telegrambot.py
